services:
  web:
    build: .
    network_mode: host
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    networks:
      - default
      - lsio

networks:
  lsio:
    external: true
